import React from "react";
import styled from "styled-components";

function ProjectAdd() {
  return <Container>Add Project</Container>;
}

const Container = styled.div`
  padding-top: 5em;
  text-align: center;
`;

export default ProjectAdd;
